import Canvas from '@/components/funnel/Canvas';

const Index = () => {
  return (
    <main className="h-screen w-screen overflow-hidden">
      <Canvas />
    </main>
  );
};

export default Index;
